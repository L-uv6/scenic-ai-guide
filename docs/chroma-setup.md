工具：
Python 版本: 3.12.7 
numpy 版本: 1.26.4
torch 版本: 2.9.0+cpu
chromadb 版本: 1.5.9
transformers 版本: 4.57.1

模型：
用huggingface的镜像源上的模型。
pip install huggingface-hub transformers -i https://pypi.tuna.tsinghua.edu.cn/simple
set HF_ENDPOINT=https://hf-mirror.com
（ps:模型暂时未确定，后续会上传拉取模型的命令行指令，或者是我上传模型，再clone）


安装指令：
pip install numpy torch chromadb transformers
（ps:基本都是最新版本的，遇到网络问题，就用清华镜像源试试）

